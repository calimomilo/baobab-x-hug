name: Deploy to production

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Create SSH key and push to bare repo
        run: |
          mkdir -p ~/.ssh/
          echo "$SSH_PRIVATE_KEY" > ../private.key
          chmod 600 ../private.key
          echo "$SSH_KNOWN_HOSTS" > ~/.ssh/known_hosts
          GIT_SSH_COMMAND="ssh -i $SSH_KEY_PATH $SSH_USER@$SSH_HOST" git push ssh://$SSH_USER@$SSH_HOST/$REMOTE_PATH main
        shell: bash
        env:
          SSH_PRIVATE_KEY: ${{secrets.SSH_PRIVATE_KEY}}
          SSH_KNOWN_HOSTS: ${{secrets.SSH_KNOWN_HOSTS}}
          SSH_USER: ${{secrets.SSH_USER}}
          SSH_HOST: ${{secrets.SSH_HOST}}
          REMOTE_PATH: ${{secrets.REMOTE_PATH}}
          SSH_KEY_PATH: ${{ github.workspace }}/../private.key

    #   - name: Push to server bare repo
    #     env:
    #       SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
    #       SSH_HOST: ${{ secrets.SSH_HOST }}
    #       SSH_USER: ${{ secrets.SSH_USER }}
    #       REMOTE_PATH: ${{ secrets.REMOTE_BARE_REPO_PATH }}
    #     run: |
    #       mkdir -p ~/.ssh
    #       echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
    #       chmod 600 ~/.ssh/id_rsa
    #       ssh-keyscan -H "$SSH_HOST" >> ~/.ssh/known_hosts
    #       git remote add production "$SSH_USER@$SSH_HOST:$REMOTE_PATH"
    #       git push production main --force
